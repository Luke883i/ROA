'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const ROOT=path.resolve(__dirname,'..','..','..');
test('semantic reticulum makes the natural-language adapter the product surface',()=>{const graph=JSON.parse(fs.readFileSync(path.join(ROOT,'Operation','SEMANTIC_RETICULUM.json'),'utf8'));assert.equal(graph.control.chat_product_adapter,'Operation/runner/semantic_chat.js');assert.equal(graph.control.surface_a_guard,'Operation/runner/surface_boundary.js');assert.deepEqual(graph.control.public_output_shape,['voice','artifact']);assert.equal(graph.control.surface_a,'plain_natural_language_prose_only');assert.equal(graph.control.surface_b,'terms_or_docx_artifact_only');});
test('runner package defaults imports to semantic chat while preserving corpus CLI',()=>{const pkg=JSON.parse(fs.readFileSync(path.join(ROOT,'Operation','runner','package.json'),'utf8'));assert.equal(pkg.main,'semantic_chat.js');assert.equal(pkg.bin['ikant-runner'],'app.js');});
